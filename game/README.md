# Disc image folder

Place the user-dumped USA `.cue` and matching `.bin` here. By default the cue must be named `Legend of Legaia (USA).cue`; edit `game.toml` if yours uses a different name.

Do not commit or upload either file.
